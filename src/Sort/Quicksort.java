/*package Sort;

import java.util.Comparator;

public class Quicksort implements Comparator<CLASS>{

        @Override
        public int compare(CLASS o1, CLASS o2) {
            int Compare_One = o1.VAR.compareTo(o2.VAR);
            if (Compare_One != 0) {
                return Compare_One;
            }
            int Compare_TWO = o1.VAR.compareTo(o2.VAR);
            if (Compare_TWO != 0) {
                return Compare_TWO;
            }
            return 1;
        }
}

//Collections.sort(LIST, new Quicksort());
*/